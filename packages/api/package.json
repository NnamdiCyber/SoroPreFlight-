{
  "name": "@soropreflight/api",
  "version": "0.1.0",
  "private": true,
  "description": "SoroPreFlight REST API server — Fastify",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@soropreflight/core": "*",
    "@soropreflight/sdk": "*",
    "fastify": "^5.2.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "eslint": "^9.20.0",
    "@types/node": "^22.0.0",
    "@types/jsonwebtoken": "^9.0.7"
  }
}
