{
  "name": "@soropreflight/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "SoroPreFlight TypeScript SDK — simulate, batch, suite runner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@soropreflight/core": "*",
    "@stellar/stellar-sdk": "^12.3.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "eslint": "^9.20.0",
    "@types/node": "^22.0.0",
    "@types/js-yaml": "^4.0.9"
  }
}
